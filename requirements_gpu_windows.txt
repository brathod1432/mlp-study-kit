# mlp-study-kit — Windows GPU requirements
# Only needed for: homework/hw_03/gpu_test.py
#
# Install:
#   pip install -r requirements_gpu_windows.txt
#
# Includes: requirements_windows.txt (which includes requirements.txt + TF)

# ── Windows runtime (base + TF) ───────────────────────────────────────────
-r requirements_windows.txt

# ── PyTorch ──────────────────────────────────────────────────────────────
# Default: CPU-only PyTorch (works on all Windows machines)
torch>=2.1,<3.0
torchvision>=0.16,<1.0
torchaudio>=2.1,<3.0

# ── CUDA (NVIDIA GPU) on Windows ─────────────────────────────────────────
# For CUDA 12.1 support, install with the NVIDIA wheel index instead:
#
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu121
#
# Check your CUDA version: nvidia-smi
# Find the right wheel: https://pytorch.org/get-started/locally/

# ── AMD GPU via DirectML (Windows only) ──────────────────────────────────
# Enables GPU acceleration on AMD, Intel, and Qualcomm hardware via DirectML.
# Uncomment the line below to install:
#
# torch-directml>=0.2
#
# Usage in code:
#   import torch_directml
#   device = torch_directml.device()
#   tensor = tensor.to(device)

# ── TensorFlow Datasets (hw03 VGG16 tasks) ───────────────────────────────
tensorflow-datasets>=4.9,<5.0
