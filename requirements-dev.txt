# mlp-study-kit -- development dependencies
# Install: pip install -r requirements-dev.txt
# (installs runtime deps too via -r requirements.txt)
-r requirements.txt

# Testing
pytest>=7.4,<9.0
pytest-cov>=4.1,<6.0

# Linting / formatting
ruff>=0.4,<1.0

# Security scanning
pip-audit>=2.6,<3.0
