# mlp-study-kit — Linux GPU requirements
# Only needed for: homework/hw_03/gpu_test.py
#
# Install:
#   pip install -r requirements_gpu_linux.txt
#
# Includes: requirements_linux.txt (which includes requirements.txt + TF)

# ── Linux runtime (base + TF) ─────────────────────────────────────────────
-r requirements_linux.txt

# ── PyTorch ───────────────────────────────────────────────────────────────
# Default: CPU-only PyTorch (works on any Linux machine)
torch>=2.1,<3.0
torchvision>=0.16,<1.0
torchaudio>=2.1,<3.0

# ── CUDA (NVIDIA GPU) on Linux ────────────────────────────────────────────
# For CUDA 12.1 support, replace the lines above with:
#
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu121
#
# Check your CUDA version:  nvidia-smi
# Find the right wheel:     https://pytorch.org/get-started/locally/
#
# Or use the conda approach for CUDA-managed environments:
#   conda install pytorch torchvision torchaudio pytorch-cuda=12.1 \
#       -c pytorch -c nvidia

# ── AMD GPU via ROCm (Linux only) ────────────────────────────────────────
# For AMD GPU on Linux with ROCm:
#
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/rocm5.7
#
# ROCm prerequisites: https://rocm.docs.amd.com/en/latest/deploy/linux/
# Check ROCm version: rocm-smi

# ── TensorFlow Datasets (hw03 VGG16 tasks) ───────────────────────────────
tensorflow-datasets>=4.9,<5.0
