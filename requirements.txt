# mlp-study-kit — cross-platform base requirements
# Python 3.10+ | Works on Windows and Linux
#
# Install (cross-platform core only):
#   pip install -r requirements.txt
#
# For OS-specific versions (includes TF, platform notes):
#   pip install -r requirements_windows.txt   (Windows)
#   pip install -r requirements_linux.txt     (Linux / macOS)
#
# For GPU support:
#   pip install -r requirements_gpu_windows.txt
#   pip install -r requirements_gpu_linux.txt
#
# For development tools (tests, lint, security):
#   pip install -r requirements_dev.txt

# ── Core numerical & plotting ─────────────────────────────────────────────
numpy>=1.26,<3.0
matplotlib>=3.8,<4.0
scipy>=1.10,<2.0

# ── ML utilities ──────────────────────────────────────────────────────────
# train_test_split used in ex10_bias_early_stop.py
scikit-learn>=1.3,<2.0

# ── Terminal colours ──────────────────────────────────────────────────────
# colorama: cross-platform ANSI colour for ObjLogger
# On Linux/macOS ANSI works natively; colorama ensures it works on Windows too
colorama>=0.4.6,<1.0

# ── Utilities ─────────────────────────────────────────────────────────────
tqdm>=4.66,<5.0
